import python_loops_and_collections # Ensure this matches your filename exactly

def test_collections():
    # Update the prefix here to match the import above!
    assert len(python_loops_and_collections.dailyTemps) == 5
    assert len(python_loops_and_collections.vehicleInventory) == 3
